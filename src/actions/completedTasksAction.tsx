// There is no functionality in this action, it's only a workaround to trigger the loader function to re-render the component with the updated data
export const completedTasksAction = async (): Promise<null> => {
  return null
}
